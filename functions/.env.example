# Copy to functions/.env.YOUR_FIREBASE_PROJECT_ID after following docs/LAUNCH.md.
PUBLIC_INTAKE_ENABLED=false
PUBLIC_ALLOWED_ORIGINS=https://roofzeus.com,https://www.roofzeus.com
# Set TURNSTILE_SECRET_KEY through firebase functions:secrets:set, never in VITE_*.
# Set MODERNIZE_SETTINGS as a JSON secret; see docs/MODERNIZE.md and the example settings file.
